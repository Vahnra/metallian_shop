{% for label, messages in app.flashes %}
    {% for message in messages %}

        <div class="alert text-center mt-2 alert-{{ label }} alert-dismissible fade show p-0" role="alert">
            {% if label == 'success' %}
                <span class="alert-inner--icon"></span>
            {% elseif label == 'Mail Vérification' %}
                <span class="alert-inner--icon"></span>
            {% elseif label == 'Attention' %}
                <span class="alert-inner--text" style="color: red"><strong>{{ label | title }} : </strong> {{ message | raw }}</span>
            {% else %}
                <span class="alert-inner--icon" style="color: red"></span>
            {% endif %}
        </div>

    {% endfor %}
{% endfor %}