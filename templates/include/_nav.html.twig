<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('default_home') }}">Metallian Store</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">

				{{ render(url('render_categories_in_nav')) }}

			</ul>
			<ul class="d-flex navbar-nav">
				{% if app.user %}

					{% if is_granted("ROLE_ADMIN") %}

						<li class="nav-item">
							<a class="nav-link fw-bold" href="{{ path('admin') }}">Menu Admin</a>
						</li>

					{% endif %}

					<li class="nav-item">
						<a class="nav-link fw-bold" href="{{ path('app_logout') }}">Se Déconnecter</a>
					</li>

				{% else %}

					<li class="nav-item">
						<a class="nav-link fw-bold" href="{{ path('app_login') }}">Se connecter</a>
					</li>

				{% endif %}

				{% if not app.user %}

					<li class="nav-item">
						<a class="nav-link fw-bold" href="{{ path('user_register') }}">S'inscrire</a>
					</li>

				{% endif %}
			</ul>
		</div>
	</div>
</nav>
